# we have a initial nunber "0"
# we can add increasing numbers to "0"
# and the number added should be "1" by "1" increased
# every number output is what we want

a=0 # Initialize sum variable "a" to 0
for i in range(1,11): # Loop from 1 to 10
    a=a+i # Add current 'i' value to 'a'
    print(a)    #Print a